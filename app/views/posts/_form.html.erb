<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div>
    <div class="field">
      <%= form.text_field :title %>
    </div>

    <div class="field">
      <%= form.collection_select(:agent_id, @agents, :id, :agent_name) %>
    </div>

    <div class="field">
      <%= form.collection_select(:map_id, @maps, :id, :map_name) %>
    </div>

    <div class="field">
      <%= form.file_field :position_image %>    
    </div>

    <div class="field">
      <%= form.file_field :angle_image %>    
    </div>

    <div class="field">
      <%= form.text_area :content %>
    </div>

    <div class="field">
      <%= form.submit %>
    </div>

  </div>
<% end %>
